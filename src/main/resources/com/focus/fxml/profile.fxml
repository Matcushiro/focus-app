<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.control.*?>

<BorderPane xmlns:fx="http://javafx.com/fxml"
            fx:controller="com.focus.controller.ProfileController"
            styleClass="root-pane">

    <center>
        <ScrollPane fitToWidth="true"
                    styleClass="main-scroll">
            <VBox spacing="30"
                  styleClass="profile-wrapper">

                <HBox spacing="25"
                      alignment="CENTER_LEFT"
                      styleClass="profile-header">
                    <StackPane styleClass="avatar-box">
                        <Label fx:id="avatarLabel"
                               styleClass="avatar-label"
                               text="👤"/>
                    </StackPane>
                    <VBox spacing="8">
                        <Label fx:id="usernameLabel"
                               styleClass="profile-name"/>
                        <Label fx:id="emailLabel"
                               styleClass="profile-email"/>
                        <HBox spacing="10">
                            <Button text="✏️ Редактировать профиль"
                                    styleClass="edit-btn"
                                    onAction="#editProfile"/>
                            <Button text="🚪 Выйти"
                                    styleClass="logout-btn"
                                    onAction="#logout"/>
                        </HBox>
                    </VBox>
                </HBox>

                <HBox spacing="20"
                      styleClass="stats-box"
                      alignment="CENTER">
                    <VBox alignment="CENTER"
                          styleClass="stat-card"
                          spacing="5">
                        <Label fx:id="watchedCount"
                               styleClass="stat-number"
                               text="0"/>
                        <Label text="Просмотрено"
                               styleClass="stat-label"/>
                    </VBox>
                    <VBox alignment="CENTER"
                          styleClass="stat-card"
                          spacing="5">
                        <Label fx:id="favoritesCount"
                               styleClass="stat-number"
                               text="0"/>
                        <Label text="В списке"
                               styleClass="stat-label"/>
                    </VBox>
                </HBox>

                <VBox fx:id="editForm"
                      spacing="15"
                      styleClass="edit-form"
                      visible="false"
                      managed="false">

                    <Label text="✏️ Редактирование профиля"
                           styleClass="form-title"/>

                    <VBox spacing="5">
                        <Label text="Имя пользователя"
                               styleClass="admin-field-label"/>
                        <TextField fx:id="editUsername"
                                   styleClass="login-field"
                                   promptText="Введите новое имя..."/>
                    </VBox>

                    <VBox spacing="5">
                        <Label text="Email"
                               styleClass="admin-field-label"/>
                        <TextField fx:id="editEmail"
                                   styleClass="login-field"
                                   promptText="Введите email..."/>
                    </VBox>

                    <VBox spacing="5">
                        <Label text="Новый пароль"
                               styleClass="admin-field-label"/>
                        <PasswordField fx:id="editPassword"
                                       styleClass="login-field"
                                       promptText="Оставьте пустым, если не меняете"/>
                    </VBox>

                    <Label fx:id="editError"
                           styleClass="error-label"
                           text=""/>

                    <HBox spacing="10">
                        <Button text="💾 Сохранить"
                                styleClass="save-btn"
                                onAction="#saveProfile"/>
                        <Button text="✖ Отмена"
                                styleClass="clear-btn"
                                onAction="#cancelEdit"/>
                    </HBox>
                </VBox>

                <VBox spacing="12" styleClass="section">
                    <Label text="❤️ Моё избранное"
                           styleClass="section-title"/>
                    <ScrollPane styleClass="h-scroll"
                                fitToHeight="true">
                        <HBox fx:id="favoritesRow"
                              spacing="12"
                              styleClass="movie-row"/>
                    </ScrollPane>
                </VBox>

                <!-- ===== ИСТОРИЯ ПРОСМОТРОВ ===== -->
                <VBox spacing="12" styleClass="section">
                    <Label text="🕐 История просмотров"
                           styleClass="section-title"/>
                    <ScrollPane styleClass="h-scroll"
                                fitToHeight="true">
                        <HBox fx:id="historyRow"
                              spacing="12"
                              styleClass="movie-row"/>
                    </ScrollPane>
                </VBox>

            </VBox>
        </ScrollPane>
    </center>

</BorderPane>
